webpack预编译：
npm run build ;  //生产模式（webpack --mode production）

npm run dev;  //开发模式（webpack --mode development）

预编译文件放在static下，（见webpack.config.js）

启动node服务器：
npm run server  ; // (webpack-dev-server)